import assert from "node:assert/strict";
import { createHash } from "node:crypto";

import { buildCurrentExitAdmissionOciReceipt } from "../build-current-exit-admission-oci-receipt.mjs";
import {
  collectKricAccessibilitySnapshots,
  KRIC_TERMINAL_RESULT_03,
} from "../collect-kric-accessibility-snapshots.mjs";

export const FIXTURE_CAPTURED_AT = new Date(0).toISOString();
export const FIXTURE_PUBLISHED_AT = new Date(1_000).toISOString();
export const FIXTURE_FRESH_UNTIL = new Date(86_400_000).toISOString();
export const FIXTURE_RETENTION_UNTIL = new Date(172_800_000).toISOString();

export async function buildCurrentCapitalStationLineInputFixture() {
  const lines = stationLines(); const stationIds = [...new Set(lines.map(({ stationId }) => stationId))].sort();
  const sourceIds = ["seoul-metro-route-map-positions", "kric-subway-timetable", "seoul-metro-accessibility", "kric-station-convenience-standard", "molit-urban-rail-full-route", "seoulmetro-station-line-info", "incheon-transit-accessibility", "seoul-metro-transfer-distance-duration"];
  const sourceSnapshots = sourceIds.map((sourceId) => {
    const snapshotId = `${sourceId}-snapshot`;
    return sourceId === "seoul-metro-transfer-distance-duration"
      ? { snapshotId, sourceId, rawObjectUri: "oci://fixture/transfer", rawSha256: "a".repeat(64), redactedRequestFingerprint: "b".repeat(64), schemaFingerprint: "c".repeat(64), licenseStatus: "PASS", redistributionAllowed: true, adminReviewRecordHash: "d".repeat(64), snapshotStatus: "LOCKED", credentialRedacted: true, freshnessExpiresAt: FIXTURE_FRESH_UNTIL, rawRetentionExpiresAt: FIXTURE_RETENTION_UNTIL, governancePolicyVersion: "fixture", governancePolicySha256: "e".repeat(64) }
      : { sourceId, snapshotId, snapshotStatus: "LOCKED" };
  });
  const snapshotIds = sourceSnapshots.map(({ snapshotId }) => snapshotId);
  const transferSnapshot = sourceSnapshots.find(({ sourceId }) => sourceId === "seoul-metro-transfer-distance-duration"); const sourceSet = fixtureSha256(JSON.stringify(sourceSnapshots)); const evidenceSourceSet = fixtureSha256(JSON.stringify(sourceSnapshots.filter(({ snapshotId }) => snapshotId !== transferSnapshot.snapshotId))); const stationSet = fixtureSha256(fixtureCanonicalJson(stationIds));
  const candidate = { candidateId: "capital-full-fixture", stationSetSha256: stationSet, sourceSetSha256: sourceSet, mappingContractVersion: "station-line-v1", materializerVersion: "1" };
  const evidenceCandidate = { ...candidate, sourceSetSha256: evidenceSourceSet };
  const snapshot = await facilitySnapshot(lines); const snapshotBytes = Buffer.from(JSON.stringify(snapshot));
  const sourceIdentity = { sourceId: snapshot.sourceId, snapshotId: snapshot.snapshotId, snapshotPath: `tools/datapack/sources/${snapshot.snapshotId}.json`, rawSha256: snapshot.rawSha256, redactedRequestFingerprint: snapshot.redactedRequestFingerprint, contentSha256: snapshot.contentSha256, schemaFingerprint: snapshot.schemaFingerprint, snapshotFileSha256: fixtureSha256(snapshotBytes), capturedAt: snapshot.capturedAt, observedAt: snapshot.observedAt, freshUntil: snapshot.freshUntil, rawObjectUri: "oci://fixture/facility", rawObjectSha256: "7".repeat(64), credentialRedacted: true, licenseEvidenceHash: "8".repeat(64) };
  const facilityAdmission = facility(lines, sourceSet, sourceIdentity);
  const normalized = { schemaVersion: 4, artifactKind: "exit-path-normalized-source-snapshot", sourceId: "kric-station-movement-standard", snapshotId: "exit-snapshot", queryPlan: lines.map((line, index) => ({ queryId: `query-${index}`, stationName: line.stationId, lineName: line.lineId, operatorName: "fixture", regionId: "capital" })), results: lines.map((_, index) => ({ queryId: `query-${index}`, providerResponseSha256: (index === 0 ? "9" : "8").repeat(64) })) };
  const exitAdmission = exit(lines, evidenceCandidate); exitAdmission.queryPartition = { joined: lines.map((line, index) => ({ stationLineId: `${line.stationId}:${line.lineId}`, queryId: `query-${index}` })) }; const mapping = lines.map((line, index) => { const query = normalized.queryPlan[index]; return { stationId: line.stationId, stationName: query.stationName, stationAliases: [], regionId: query.regionId, lineId: line.lineId, lineName: query.lineName, operatorId: "seoul-metro", operatorName: query.operatorName }; }).sort((a, b) => a.stationId.localeCompare(b.stationId) || a.lineId.localeCompare(b.lineId)); exitAdmission.stationLineMappingSha256 = fixtureSha256(fixtureCanonicalJson(mapping)); resealFixtureFacilityAdmission(exitAdmission); const exitNormalizedBytes = Buffer.from(fixtureCanonicalJson(normalized)); const exitAdmissionBytes = Buffer.from(fixtureCanonicalJson(exitAdmission));
  const exitReceipt = receipt(exitNormalizedBytes, exitAdmissionBytes);
  const metrics = transferMetrics(lines); const applicability = transferApplicability(lines, metrics);
  const transferAdmissionEvidence = { decision: "APPROVED", metricsArtifactSha256: metrics.artifactSha256, applicabilityArtifactSha256: applicability.artifactSha256, physicalPairCount: metrics.physicalPairs.length, directedMetricCount: metrics.metrics.length, officialMetricCount: metrics.metrics.filter(({ metricProvenance }) => metricProvenance === "OFFICIAL_SOURCE").length, derivedReciprocalMetricCount: metrics.metrics.filter(({ metricProvenance }) => metricProvenance === "DERIVED_RECIPROCAL").length, durationRole: "REFERENCE_ONLY", snapshotId: transferSnapshot.snapshotId, freshUntil: FIXTURE_FRESH_UNTIL, licenseEvidenceHash: "9".repeat(64) };
  const sourceInventory = { sources: sourceIds.map((id) => id === "seoul-metro-transfer-distance-duration"
    ? { id, requiredForProductionPack: true, admissionEvidence: { adminReviewRecordHash: "d".repeat(64) }, transferAdmissionEvidence }
    : { id, requiredForProductionPack: true }) }; const sourceInventoryBytes = Buffer.from(fixtureCanonicalJson(sourceInventory));
  const candidateBuildSpec = { candidateId: candidate.candidateId, publishedAt: FIXTURE_PUBLISHED_AT, sourceSnapshotIds: snapshotIds, sourceSnapshots: sourceSnapshots.map((entry) => entry.snapshotId === transferSnapshot.snapshotId ? Object.fromEntries(["snapshotId", "sourceId", "rawObjectUri", "rawSha256", "redactedRequestFingerprint", "schemaFingerprint", "licenseStatus", "redistributionAllowed", "adminReviewRecordHash", "snapshotStatus", "credentialRedacted", "freshnessExpiresAt", "rawRetentionExpiresAt", "governancePolicyVersion", "governancePolicySha256"].map((key) => [key, entry[key]])) : ({ snapshotId: entry.snapshotId, sourceId: entry.sourceId })), sourceSnapshotSetHash: fixtureSha256(JSON.stringify(sourceSnapshots)), sourceInventorySha256: fixtureSha256(JSON.stringify(sourceInventory)), networkEdgeEvidence: { sourceInventory: { path: "tools/datapack/source-inventory.json", sha256: fixtureSha256(sourceInventoryBytes) } } };
  const lineIds = [...new Set(lines.map(({ lineId }) => lineId))];
  return { canonicalPack: { manifest: { channel: "production", activePack: { id: "capital" } }, packs: [{ id: "capital", sourceInventory: sourceIds.map((id) => ({ id })), lines: lineIds.map((id) => ({ id, operatorId: "seoul-metro" })), stationLines: lines.map((line, lineSequence) => ({ ...line, lineSequence })) }] }, candidateBuildSpec, exitAdmission, exitAdmissionBytes, exitNormalized: normalized, exitNormalizedBytes, exitReceipt, facilityAdmission, facilitySnapshotBytes: snapshotBytes, policy: { artifactKind: "route-edge-evaluation-policy", policyVersion: "route-edge-evaluation-v2", edgeDomainMap: { RIDE: { endpointTarget: "NONE" } } }, sourceInventory, sourceInventoryBytes, sourceSnapshots, sourceSetTransition: { currentCandidateBytesSha256: "1".repeat(64), currentCandidateSourceSetSha256: sourceSet, evidenceSourceSetSha256: evidenceSourceSet, facilityAdmissionBytesSha256: "2".repeat(64) }, transferMetrics: metrics, transferApplicability: applicability };
}

function stationLines() { return [{ stationId: KRIC_TERMINAL_RESULT_03.stationId, lineId: KRIC_TERMINAL_RESULT_03.lineId }, { stationId: "station-z-fixture-transfer", lineId: "fixture-a" }, { stationId: "station-z-fixture-transfer", lineId: "fixture-b" }]; }
async function facilitySnapshot(lines) { const blocked = lines[0]; const roster = lines.map((line, index) => ({ ...line, ...(line === blocked ? KRIC_TERMINAL_RESULT_03 : { railOprIsttCd: "S2", lnCd: line.lineId, stinCd: String(index) }), canonicalMappings: [{ artifactId: "fixture", ...line }] })); const [snapshot] = await collectKricAccessibilitySnapshots({ roster, operations: [{ sourceId: "kric-station-convenience-standard", endpoint: "https://openapi.kric.go.kr/openapi/handicapped/stationCnvFacl", responseFields: ["dtlLoc", "grndDvCd", "gubun", "imgPath", "mlFmlDvCd", "stinFlor", "trfcWeakDvCd"], tupleIdentityFields: [] }], serviceKey: "fixture", now: new Date(FIXTURE_CAPTURED_AT), allowTerminalResult03: true, fetchImpl: async (url) => url.searchParams.get("stinCd") === KRIC_TERMINAL_RESULT_03.stinCd ? ({ ok: true, status: 200, json: async () => ({ header: { resultCode: "03" }, body: [] }) }) : ({ ok: true, status: 200, json: async () => ({ header: { resultCode: "00" }, body: [{ dtlLoc: "x", grndDvCd: "1", gubun: "EV", imgPath: "", mlFmlDvCd: "", stinFlor: 1, trfcWeakDvCd: "01" }] }) }) }); return snapshot; }
function facility(lines, sourceSet, sourceIdentity) { const blocked = lines[0]; const cells = lines.map((line) => ({ ...line, state: line === blocked ? "ADMITTED_FACILITY_UNVERIFIED_BLOCKED" : "ADMITTED_FACILITY_PRESENT", sourceId: sourceIdentity.sourceId, snapshotId: sourceIdentity.snapshotId })); const denominatorRows = cells.flatMap((cell) => ["ELEVATOR", "ESCALATOR", "WHEELCHAIR_LIFT"].map((facilityType) => ({ stationId: cell.stationId, lineId: cell.lineId, facilityType, state: cell.state === "ADMITTED_FACILITY_UNVERIFIED_BLOCKED" ? "UNVERIFIED_EVIDENCE_BLOCKED" : "VERIFIED_PRESENT", sourceId: cell.sourceId, snapshotId: cell.snapshotId }))); const value = { schemaVersion: 1, artifactKind: "current-capital-facility-source-admission", observedAt: FIXTURE_CAPTURED_AT, candidate: { candidateId: "capital-full-fixture", sourceSnapshotSetHash: sourceSet }, sourceIdentity, stationLineProviderMappingSha256: "a".repeat(64), denominatorRows, denominatorStateSummary: summarize(denominatorRows, ["VERIFIED_PRESENT", "VERIFIED_ABSENT", "UNVERIFIED_EVIDENCE_BLOCKED"]), cells, cellStateSummary: summarize(cells, ["ADMITTED_FACILITY_PRESENT", "ADMITTED_FACILITY_ABSENT", "ADMITTED_FACILITY_UNVERIFIED_BLOCKED"]), materializerEvidenceRows: cells.map((cell) => ({ ...cell, evidenceState: cell.state === "ADMITTED_FACILITY_UNVERIFIED_BLOCKED" ? "UNVERIFIED_EVIDENCE_BLOCKED" : "VERIFIED_PRESENT" })), decision: "GO" }; resealFixtureFacilityAdmission(value); return value; }
function exit(lines, candidate) {
  const projection = lines.map((line) => ({ ...line, operatorId: "seoul-metro" })).sort((left, right) => left.stationId.localeCompare(right.stationId) || left.lineId.localeCompare(right.lineId));
  const projectionSha256 = fixtureSha256(fixtureCanonicalJson(projection));
  const evidence = projection.map((line, index) => {
    const base = { ...candidate, ...line, domain: "EXIT", sourceId: "kric-station-movement-standard", sourceSnapshotId: "exit-snapshot", evidenceRawSha256: "b".repeat(64), capturedAt: FIXTURE_CAPTURED_AT, freshUntil: FIXTURE_FRESH_UNTIL, provenanceId: "d".repeat(64), licenseId: "e".repeat(64) };
    if (index !== 0) return { ...base, state: "VERIFIED_PRESENT", providerRecordHash: "c".repeat(64), evidenceKind: "OBSERVED", evidenceReason: "fixture" };
    const providerResponseSha256 = "9".repeat(64); const terminalPolicy = "PROVIDER_NO_DATA_RESULT_03_BLOCKED";
    const evidenceHash = fixtureSha256(fixtureCanonicalJson({ sourceSnapshotId: base.sourceSnapshotId, stationId: line.stationId, lineId: line.lineId, operatorId: line.operatorId, domain: "EXIT", terminalPolicy, providerResponseSha256 }));
    return { ...base, state: "UNVERIFIED_EVIDENCE_BLOCKED", providerRecordHash: null, evidenceKind: "UNVERIFIED_EVIDENCE_BLOCKED", evidenceReason: "출구 이동경로가 검증되지 않아 경로를 차단했습니다.", terminalPolicy, providerResultCode: "03", strictRouteEligible: false, strictRouteEligibleReason: "UNVERIFIED_PROVIDER_EVIDENCE_BLOCKED", statusMeaning: "PROVIDER_NO_DATA_NOT_ABSENCE", confidence: 0, providerResponseSha256, evidenceHash };
  });
  const payload = { schemaVersion: 2, artifactKind: "exit-path-admission-matrix", candidate, sourceIdentity: { sourceId: "kric-station-movement-standard", snapshotId: "exit-snapshot", rawSha256: "b".repeat(64) }, stationLineMappingSha256: projectionSha256, stationLineSetSha256: projectionSha256, normalizedEvidenceSha256: "1".repeat(64), queryPartition: {}, cells: [], materializerEvidenceRows: evidence, stateSummary: { ADMITTED_EXIT_PATH: evidence.filter(({ state }) => state !== "UNVERIFIED_EVIDENCE_BLOCKED").length, ADMITTED_EXIT_UNVERIFIED_BLOCKED: evidence.filter(({ state }) => state === "UNVERIFIED_EVIDENCE_BLOCKED").length }, decision: "GO" };
  return { ...payload, admissionDigest: fixtureSha256(fixtureCanonicalJson(payload)) };
}
function receipt(normalizedBytes, admissionBytes) { const provider = Buffer.from("fixture-provider"); const operationId = "current-capital-fixture"; const mainSha = "a".repeat(40); return buildCurrentExitAdmissionOciReceipt({ repository: "AquilaXk/easysubway-data", mainSha, operationId, providerCapturedAt: FIXTURE_CAPTURED_AT, providerCollectionBundleBytes: provider, providerObjectUri: `oci://axvym6vk8g7i/easysubway-datapacks/operations/current-capital-live-chain/v1/heads/${mainSha}/operations/${operationId}/provider-collections/19700101-${fixtureSha256(provider)}.json`, providerObjectSha256: fixtureSha256(provider), providerObjectByteSize: provider.length, normalizedBytes, admissionBytes }); }
function transferMetrics(lines) { const grouped = Map.groupBy(lines, ({ stationId }) => stationId); const pair = [...grouped.entries()].find(([, stationLines]) => stationLines.length === 2); assert.ok(pair); const [stationId, stationLines] = pair; const lineIds = stationLines.map(({ lineId }) => lineId); const physicalPairs = [{ stationId, lineIds }]; const metrics = lineIds.map((fromLineId, index) => ({ stationId, fromLineId, toLineId: lineIds[1 - index], distanceMeters: 1, officialDurationSecondsReference: 1, durationRole: "REFERENCE_ONLY", metricProvenance: "DERIVED_RECIPROCAL" })); const canonicalIdentity = { stationLineCount: lines.length, stationCount: new Set(lines.map((line) => line.stationId)).size, physicalPairCount: physicalPairs.length }; const sourceIdentity = { sourceId: "seoul-metro-transfer-distance-duration", rawSha256: "f".repeat(64), capturedAt: FIXTURE_CAPTURED_AT }; const payload = { artifactKind: "current-transfer-topology-metrics", physicalPairs, metrics, canonicalIdentity, sourceIdentity }; return { ...payload, artifactSha256: fixtureSha256(fixtureCanonicalJson(payload)) }; }
function transferApplicability(lines, metrics) { const endpoints = new Set(metrics.metrics.flatMap((metric) => [`${metric.stationId}\0${metric.fromLineId}`, `${metric.stationId}\0${metric.toLineId}`])); const cells = lines.map((line) => ({ ...line, state: endpoints.has(`${line.stationId}\0${line.lineId}`) ? "APPLICABLE_TRANSFER_ENDPOINT" : "NOT_APPLICABLE_IN_CANONICAL_PAIR_SET" })); const payload = { artifactKind: "current-capital-transfer-topology-applicability-pre-candidate", candidateBinding: null, productionUseAllowed: false, canonicalIdentity: metrics.canonicalIdentity, sourceIdentity: metrics.sourceIdentity, transferTopologyMetricsIdentity: { artifactSha256: metrics.artifactSha256 }, cells }; return { ...payload, artifactSha256: fixtureSha256(`${fixtureCanonicalJson(payload)}\n`) }; }
export function resealFixtureFacilityAdmission(value) { const { admissionDigest: _ignored, ...payload } = value; value.admissionDigest = fixtureSha256(fixtureCanonicalJson(payload)); }
export function fixtureCanonicalJson(value) { if (Array.isArray(value)) return `[${value.map(fixtureCanonicalJson).join(",")}]`; if (value && typeof value === "object") return `{${Object.keys(value).sort().map((key) => `${JSON.stringify(key)}:${fixtureCanonicalJson(value[key])}`).join(",")}}`; return JSON.stringify(value); }
export function fixtureSha256(value) { return createHash("sha256").update(value).digest("hex"); }
function summarize(rows, states) { return Object.fromEntries(states.map((state) => [state, rows.filter((row) => row.state === state).length])); }
